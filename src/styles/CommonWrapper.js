const CommonStyles = (theme) => ({
  floatingBtn: {
    margin: "10px",
    zIndex: 999,
    position: "fixed",
    bottom: 0,
    right: 0,
  },
  floatingPadding: {
    padding: theme.spacing(1),
  },
});
export default CommonStyles;
