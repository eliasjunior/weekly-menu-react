import React from 'react';
import RecipePage from './RecipePage';
import { shallow } from 'enzyme';

describe("RecipePage", () => {
    it("should load component", () => {
        // shallow(<RecipePage location={{pathname: ''}} recipeId='1' />);
    });
});